# Use a hoisted node_modules layout for compatibility with some tooling on Windows
# (avoids resolution issues with symlinked pnpm layout under Turbopack)
node-linker=hoisted

auto-install-peers=true
